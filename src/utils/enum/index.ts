export enum EChangeType {
  CREATE = 'create',
  UPDATE = 'update',
  ROLLBACK = 'rollback',
  DELETE = 'delete',
  RESTORE = 'restore',
}

export enum EGender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum ERole {
  ADMIN = 'admin',
  PATIENT = 'patient',
}

export enum EStatus {
  ACTIVE = 'active',
  TRASH = 'trash',
}
